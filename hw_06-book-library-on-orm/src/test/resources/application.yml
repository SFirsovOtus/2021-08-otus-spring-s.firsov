spring:
  datasource:
    url: jdbc:h2:mem:testdb
    data: test-data.sql
  sql:
    init:
      mode: always
      encoding: UTF-8
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
  shell:
    interactive:
      enabled: false
